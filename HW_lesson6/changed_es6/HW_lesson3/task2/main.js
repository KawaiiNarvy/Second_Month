function twoMass(arr, arr2) {

    if (arr.length< arr2.length){
        console.log(`${arr} < ${arr2}`)
    } else {
        console.log(`${arr} > ${arr2}`)
    }
}

const arr = ["Mia", "lana", "Joe"]
const arr2 = [1, 5,]

twoMass(arr, arr2);